ASSIGNMENT - 5
(Using default Oracle tables EMP and DEPT)

1. List all the employee names, dept name and the city, in department name
   order.
SELECT E.ENAME, D.DNAME, D.LOC
FROM EMP E, DEPT D
WHERE E.DEPTNO = D.DEPTNO
ORDER BY D.DNAME;

2. List all employees working in Dallas in descending order of salary.
SELECT E.*
FROM EMP E, DEPT D
WHERE E.DEPTNO = D.DEPTNO
AND D.LOC = 'DALLAS'
ORDER BY E.SAL DESC;

3. List employee name, department name, job and location of all employees who
   work in DALLAS.
SELECT E.ENAME, D.DNAME, E.JOB, D.LOC
FROM EMP E, DEPT D
WHERE E.DEPTNO = D.DEPTNO
AND D.LOC = 'DALLAS';

4. List the employee name, salary, PF, HRA, DA and gross salary; order the
   result in ascending order of gross. PF is 10% of salary, HRA is 60% of
   salary and DA is 40% of salary.
SELECT ENAME, SAL,
       (SAL * 0.10) AS PF,
       (SAL * 0.60) AS HRA,
       (SAL * 0.40) AS DA,
       (SAL + (SAL * 0.60) + (SAL * 0.40) - (SAL * 0.10)) AS GROSS_SALARY
FROM EMP
ORDER BY GROSS_SALARY ASC;

5. Display names and salary of all the employees who report to KING.
SELECT ENAME, SAL
FROM EMP
WHERE MGR = (SELECT EMPNO FROM EMP WHERE ENAME = 'KING');

6. List all employees who work in DALLAS and earn more than any employee
   working in Chicago.
SELECT *
FROM EMP
WHERE DEPTNO = (SELECT DEPTNO FROM DEPT WHERE LOC = 'DALLAS')
AND SAL > ANY (SELECT SAL FROM EMP
               WHERE DEPTNO = (SELECT DEPTNO FROM DEPT WHERE LOC = 'CHICAGO'));

7. List all employees who work in the same post as Smith.
SELECT *
FROM EMP
WHERE JOB = (SELECT JOB FROM EMP WHERE ENAME = 'SMITH')
AND ENAME != 'SMITH';

8. Find the job with the highest average salary.
SELECT JOB, AVG(SAL) AS Avg_Salary
FROM EMP
GROUP BY JOB
ORDER BY Avg_Salary DESC
FETCH FIRST 1 ROW ONLY;

-- Alternative (older Oracle versions without FETCH):
-- SELECT JOB, Avg_Salary FROM (
--   SELECT JOB, AVG(SAL) AS Avg_Salary
--   FROM EMP
--   GROUP BY JOB
--   ORDER BY Avg_Salary DESC
-- ) WHERE ROWNUM = 1;

9. List the top 10 earners in the company.
SELECT *
FROM (SELECT * FROM EMP ORDER BY SAL DESC)
WHERE ROWNUM <= 10;

10. Display the names of all the employees' replacing 'A' with 'a'.
SELECT REPLACE(ENAME, 'A', 'a') AS ENAME
FROM EMP;

11. Show the salary of all the employees rounding it to the nearest
    Rs.1000/-.
SELECT ENAME, ROUND(SAL, -3) AS Rounded_Salary
FROM EMP;

12. Show the first three and last three characters of the names of all the
    employees.
SELECT ENAME,
       SUBSTR(ENAME, 1, 3) AS First_Three,
       SUBSTR(ENAME, -3) AS Last_Three
FROM EMP;
